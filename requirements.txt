fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.40
pydantic==2.11.1
psycopg2-binary==2.9.10
python-dotenv==1.2.2
PyJWT==2.10.1
# Required for the key-file login: FastAPI needs it to parse multipart uploads.
python-multipart==0.0.20
# Diary photo downscaling (app/services/image_optimize.py). Imported lazily, so
# the app still runs if it is ever missing — uploads just skip optimisation.
pillow==11.1.0
