DATABASE_URL=postgresql://bektas:bektas@localhost:5432/bektas_dev
SUPABASE_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres

# The Apple Shortcut that uploads sleep every morning. A long random string,
# scoped to POST /api/health/sleep and nothing else — it is NOT the admin key
# (see CLAUDE.md "Sleep"). Unset, that endpoint answers 503 and says so.
HEALTH_INGEST_TOKEN=a-long-random-string
